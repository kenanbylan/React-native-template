import { WIDTH } from "./Dimension";

export const mySize = {
  size20: WIDTH / 20,
  size30: WIDTH / 30,
  size40: WIDTH / 40,
  size50: WIDTH / 50,
  size2: WIDTH / 2,
  size3: WIDTH / 3,
  size4: WIDTH / 4,
  size5: WIDTH / 5,
  size6: WIDTH / 6,
  size7: WIDTH / 7,
  size8: WIDTH / 8,
  size9: WIDTH / 9,
  size10: WIDTH / 10,
  size11: WIDTH / 11,
  size12: WIDTH / 12,
  size13: WIDTH / 13,
  size14: WIDTH / 14,
  size15: WIDTH / 15,
  size16: WIDTH / 16,
  size17: WIDTH / 17,
  size18: WIDTH / 18,
  size19: WIDTH / 19,
  size21: WIDTH / 21,
  size27: WIDTH / 27,
  size28: WIDTH / 28,
  size25: WIDTH / 25,
  size32: WIDTH / 32,
  size34: WIDTH / 34,
  size35: WIDTH / 35,
};

export default {
  Colors,
};
